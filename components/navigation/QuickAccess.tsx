import React from 'react';

export function QuickAccess(): React.ReactElement {
  const [notifications, _setNotifications] = React.useState<number>(0);

  return (
    <div>
      {/* QuickAccess implementation */}
    </div>
  );
}
